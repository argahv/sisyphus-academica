# Sisyphus Academica — Python dependencies
# Install: pip install -r requirements.txt

# Core tools (stdlib-only for citation_verifier.py + literature_client.py)
# No external deps needed for the Python tools — they use urllib, json, xml, re

# Agent-defined packages used at runtime (installed by data-engineer subagent)
# These are referenced in agent prompts, not imported by the Python tools:
scienceplots>=2.0     # Publication-quality matplotlib styles
seaborn>=0.12         # Statistical data visualization
scipy>=1.10           # Statistical tests, signal processing
scikit-learn>=1.3     # ML metrics, preprocessing
statsmodels>=0.14     # Statistical modeling
matplotlib>=3.7       # Base plotting

# HTTP (optional — tools work with stdlib urllib, but requests improves reliability)
requests>=2.31

# Development
pytest>=7.4           # Test runner
pytest-mock>=3.15.1     # Mocking for API tests
